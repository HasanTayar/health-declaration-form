import { FormComponentsProps } from '@/constants'

const FormComponents = ({formData , handleInputChange}:FormComponentsProps) => {
  return (
    <div>
      
    </div>
  )
}

export default FormComponents
