export interface checkForExistingPDFProps {
  id: string;
}
export type UserType = {
  firstName: string;
  secondName: string;
  id: string;
  expiredAt: string;
};
export type AdminType = {
  firstName: string;
  secondName: string;
  id: string;
};
